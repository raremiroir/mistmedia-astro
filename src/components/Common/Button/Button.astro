---
import Button from "./Button.svelte";
import type { ButtonColors, ButtonGroupType, ButtonSizes, ButtonVariant } from "./button";

export interface Props {
	href?: string;
   variant?: ButtonVariant;
   color?: ButtonColors;
   size?: ButtonSizes;
   group?: ButtonGroupType;
   class?: string;

   disabled?: boolean;
   block?: boolean;
   active?: boolean;
   // loading?: boolean;
   rounded?: boolean;
   circle?: boolean;
   square?: boolean;
   tile?: boolean;
   flat?: boolean;
   // icon?: boolean;
   // iconOnly?: boolean;
   // iconRight?: boolean;
   // iconLeft?: boolean;
}

const { date } = Astro.props;
---


<Button 
   client:idle>
   <slot/>
</Button>