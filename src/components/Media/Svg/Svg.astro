---
import Svg from "./Svg.svelte";

export interface Props {
   logo?: boolean;
   animIcon?: string;
   svg?: string;

   size?: number;
   klass?: string;
}

const { logo, animIcon, svg, size, klass } = Astro.props;
---

<Svg size={size} class={klass} logo={logo} animIcon={animIcon} svg={svg}/>