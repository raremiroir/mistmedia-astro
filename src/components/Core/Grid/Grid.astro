---
export interface Props {
   cols?: number;
   rows?: number;
   gap?: string;
}
const { cols, rows, gap } = Astro.props;


// Classes
---

<ul class={` grid-rows
   grid
   ${cols ? `grid-cols-${cols}` : 'grid-cols-3'}
   ${rows ? `grid-rows-${rows}` : ''}
   ${gap ? `gap-${gap}` : 'gap-2'}
`}>
   <slot/>
</ul>